import { EmptyState } from '@/components/ui';

export default function AdminHomePage() {
  return (
    <EmptyState
      title="Административная панель"
      description="Отдельный контур для управления курсами, заказами и пользователями будет подключен позже."
    />
  );
}
