export function buildPublicCoursePath(slug: string) {
  return `/course/${slug}`;
}

export function buildCatalogPath() {
  return '/courses';
}

export function buildAppCoursePath(slug: string) {
  return `/app/courses/${slug}`;
}
